@page
@model LandingWebSite.Pages.AbountModel
@{ await Model.RenderPageAsync(this); }

<h1>@Model.Title</h1>

<div>
    <h4 content-text="ContentModel.Title">test</h4>

    <a href="#test">link with hash</a>
</div>

@await Html.BlockAsync("test", typeof(Contents.TextBlock.TB1))

@await Html.BlockAsync("test2", typeof(Contents.BannerBlock.BB1))